packages:
  - .

# NOTE: this file must be copied into the Docker deps stage (see Dockerfile),
# otherwise pnpm install --frozen-lockfile fails on unapproved build scripts
# and mismatched overrides.

# Allow all dependency build scripts (better-sqlite3, esbuild, sharp, etc.)
dangerouslyAllowAllBuilds: true

overrides:
  # immer 11.1.9 (transitive via recharts) is too fresh for the deploy's
  # minimumReleaseAge supply-chain policy; pin to an aged release.
  immer: 11.1.8
